# Scenarios

Scenario content will live here once the V2 directory loader is defined.

Implemented first vertical slice: `cuban_missile_crisis_1962`.

For now the playable scenario is hard-wired in
`src/crisis_room/scenario/schema.py` as
`build_cuban_missile_crisis_1962_scenario()`. The directory under
`scenarios/` is a design placeholder for the eventual JSON/data-driven form.
